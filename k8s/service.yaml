apiVersion: v1
kind: Service
metadata:
  name: dailyops
  labels:
    app: dailyops
spec:
  type: ClusterIP
  selector:
    app: dailyops
  ports:
    - name: http
      port: 80
      targetPort: 3000
